apiVersion: v1
kind: Service
metadata:
  name: {{ include "atlas-front.fullname" . }}
  labels:
    {{- include "atlas-front.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atlas-front.selectorLabels" . | nindent 4 }}
