apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "atlas-front.fullname" . }}-test-connection"
  labels:
    {{- include "atlas-front.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "atlas-front.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
